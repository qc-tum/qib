[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "qib"
version = "0.0.1"
readme = "README.rst"
license = { file = "LICENSE" }
requires-python = ">=3.7"
dependencies = [
    "numpy",
    "scipy",
]

[project.urls]
Homepage = "https://github.com/qc-tum/qib"
